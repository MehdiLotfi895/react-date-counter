# 📅 React Date Counter

A lightweight React application designed to practice state management and dynamic date calculations.

## 🚀 Key Features
- **Step & Count Logic:** Uses `useState` to manage step increments and the total day count.
- **Live Date Calculation:** Automatically calculates dates in the future or past based on the count.
- **Modern Styling:** Features a clean UI with custom CSS for buttons and layout.

## 🛠️ Tech Stack
- **Library:** React.js
- **Language:** JavaScript (ES6+)
- **Styling:** CSS3

## ⚙️ Setup
1. Clone the repo
2. Run `npm install`
3. Run `npm start`
